#include "glm/glm.hpp"

namespace glm {
  bool operator<(vec2 vec1, vec2 vec2);
  bool operator>(vec2 vec1, vec2 vec2);
  bool operator<=(vec2 vec1, vec2 vec2);
  bool operator>=(vec2 vec1, vec2 vec2);
}
